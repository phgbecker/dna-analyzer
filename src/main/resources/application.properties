# Default API PATH
server.servlet.context-path=/rest

# Enables H2 web console
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Data source
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.url=jdbc:h2:mem:dna;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=MYSQL
spring.datasource.sqlScriptEncoding=UTF-8
spring.datasource.username=dna
spring.datasource.password=3qcVaWbMQFNFTuHg

# Database
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.database=H2

# SQL queries formatting
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.generate-ddl=false

# Logging
logging.level.org.hibernate.type.descriptor=trace
logging.level.org.hibernate.tool.schema.internal.ExceptionHandlerLoggedImpl=ERROR
